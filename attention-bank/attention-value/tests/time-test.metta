!(register-module! ../../../../metta-attention)
!(import! &self metta-attention:attention:agents:mettaAgents:AttentionParam)
!(import! &self metta-attention:attention-bank:attention-value:getter-and-setter)
!(import! &self metta-attention:attention-bank:bank:atom-bins:atombins)
!(import! &self metta-attention:attention-bank:bank:attentional-focus:attentional-focus)
!(import! &self metta-attention:attention-bank:bank:importance-index:importance-index)
!(import! &self metta-attention:attention-bank:utilities:helper-functions)
!(import! &self metta-attention:attention-bank:bank:attention-bank)
! (bind! time-mod (py-atom time))
!(bind! timeTime (py-dot time-mod time (-> Number)))
!(bind! firstTime (timeTime))
!(stimulate A 1500.0)
!(bind! secondTime (timeTime))
!(stimulate c 1500.0)
!(bind! thirdTime (timeTime))
!(stimulate d 290.0)
!(bind! fourthTime (timeTime))
!(thirdTime)
!(fourthTime)
!(stim (-  secondTime firstTime))
!(stim (-  thirdTime secondTime))
!(stim (-  fourthTime thirdTime))
!(bind! AVfirstTime (timeTime))
!(setAv G (200 200 0))
!(bind! AVsecondTime (timeTime))
!(aten value(-  AVsecondTime AVfirstTime))
!(bind! AVfirstimpoTime (timeTime))
!(updateImportance (AtomBin) a 3.0 5.0)
!(bind! AVsecondimpoTime (timeTime))
!(impo (-  AVsecondimpoTime AVfirstimpoTime))
!(bind! firstUpdateTime (timeTime))
!(update (AtomBin))
!(bind! secondupdateTime (timeTime))
!(updated (- secondupdateTime firstUpdateTime))
!(bind! firstanUpdateTime (timeTime))
!(update (AtomBin))
!(bind! secondanupdateTime (timeTime))
!(updated (- secondanupdateTime firstanUpdateTime))
!(bind! firstanimpoTime (timeTime))
!(updateImportance (AtomBin) j 3.0 5.0)
!(bind! secondanimpoTime (timeTime))
!(impo (-  secondanimpoTime firstanimpoTime))
!(bind! firstAFTime (timeTime))
!(updateAttentionalFocus k)
!(bind! secondAFimpoTime (timeTime))
!(AF (-  secondAFimpoTime firstAFTime))
!(bind! firstanAFTime (timeTime))
!(updateAttentionalFocus H)
!(bind! secondanAFimpoTime (timeTime))
!(AF (-  secondanAFimpoTime firstanAFTime))
